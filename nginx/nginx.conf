events {}

http {
    upstream active {
        server blue:5000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://active;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
